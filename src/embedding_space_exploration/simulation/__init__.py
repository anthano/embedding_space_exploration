"""Tier 0: synthetic embedding spaces with known structure.

The instrument has to be validated before it is trusted. Every check in
``battery`` claims to detect something; this package builds spaces where that
something is *planted*, so the harness can ask each check whether it fires when
it should, stays quiet when it should, and where in between it stops working.
Nothing here reads real data.
"""
